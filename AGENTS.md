# AGENTS.md

## 开发约束
- index.html 是当前唯一的网页入口，也是 dev 分支唯一允许修改的 HTML 主页面。
- index-v2.html 和 v2-working.html 仅作为历史参考文件保留，禁止继续修改，也不要删除。
- 日常开发只在 dev 分支进行；测试确认后才合并到 main。
- 不得破坏现有知识库数据、搜索、收藏、最近使用、导入导出和退款计算器。
- 涉及服务条款、合规词表、客服 SOP、on-hold 退款及质保规则时，不得自行改变业务口径。
- 每次只完成一个清晰的小任务；修改后先检查 git diff 和 git status，不要自动提交或推送。

## 适用场景
- 需要修改页面或交互时，优先确认目标是否符合上述入口与分支约束。
- 涉及业务规则或数据结构的改动时，必须保持现有行为稳定，避免引入回归。
